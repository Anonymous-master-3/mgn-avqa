model:
  visual_dim: 12
  question_dim: 10
  audio_dim: 8
  hidden_dim: 32
  max_visual_len: 5
  max_question_len: 6
  max_audio_len: 8
  max_answer_len: 3
  graph_layers: 2
  encoder_heads: 4
  encoder_conv_layers: 2
  encoder_kernel_size: 3
  dropout: 0.0
  tpa_rank: 8
  tpa_feature_rank: 8
  bilinear_rank: 16
  variant: mgn
  contrastive: true
  contrastive_chunk_size: 8
training:
  lr: 0.005
  batch_size: 4
  epochs: 30
  seed: 42
  warmup_steps: 0
  early_stopping_patience: 5
  grad_clip: 5.0
  contrastive_weight: 0.01
  num_workers: 0
  device: cpu
  smoke_steps: 300
  smoke_samples: 4
